import { DefaultPagination } from '../../types/types';

const defaultWinnersPagination = {
  limit: DefaultPagination.limitWinners,
  amountItems: DefaultPagination.amountItems,
  currentPage: DefaultPagination.currentPage,
};

export default defaultWinnersPagination;
